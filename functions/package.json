{
    "name": "functions",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "deploy": "wrangler deploy",
        "dev": "wrangler dev --test-scheduled",
        "start": "wrangler dev --test-scheduled",
        "cf-typegen": "wrangler types",
        "db:generate": "drizzle-kit generate",
        "db:migrate": "wrangler d1 migrations apply threads-ai-db --local",
        "db:migrate:prod": "wrangler d1 migrations apply threads-ai-db --remote"
    },
    "devDependencies": {
        "@types/node": "^25.9.3",
        "drizzle-kit": "^0.31.10",
        "typescript": "^5.5.2",
        "wrangler": "^4.102.0"
    },
    "dependencies": {
        "drizzle-orm": "^0.45.2",
        "hono": "^4.12.27"
    },
    "overrides": {
        "esbuild": ">=0.28.0"
    }
}
